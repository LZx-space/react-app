import React from 'react';
import PropTypes from 'prop-types';

Home.propTypes = {
    id: PropTypes.number.isRequired,
};

function Home(props) {
    return (
        <div>
            
        </div>
    );
}

export default Home